import styled from 'styled-components';

export const Logo = styled.div`
  display: flex;
  align-items: center;
  height: 100%;

  h1,
  a {
    display: flex;
    height: fit-content;
  }
`;

export default Logo;
